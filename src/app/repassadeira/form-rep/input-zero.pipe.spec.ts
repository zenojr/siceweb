import { InputZeroPipe } from './input-zero.pipe';

describe('InputZeroPipe', () => {
  it('create an instance', () => {
    const pipe = new InputZeroPipe();
    expect(pipe).toBeTruthy();
  });
});
